{
  "buildCommand": "ng build --configuration production --ssr false",
  "outputDirectory": "dist/devlog-frontend/browser",
  "installCommand": "npm install",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
